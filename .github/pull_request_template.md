<!-- Base branch is `develop`. `main` only takes release pull requests from `develop`. -->

## What

## Checklist

- [ ] Browser-facing change ships its extension half in `extension/` and an e2e case in `src/e2e/`
- [ ] README command reference is current
